version: "3.5"

services:
  postgres-service:
    container_name: postgres-service
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 'postgres'
      POSTGRES_DB: postgres
    expose:
      - 5432
    ports:
      - 5432:5432
